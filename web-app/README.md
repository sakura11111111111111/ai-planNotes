# AI Plan Notes - Web Frontend

智能复习笔记系统的 React Web 前端应用。

## 📋 项目简介

这是一个基于 React 18 + TypeScript 构建的现代化 Web 应用，对接 Spring Boot 后端 API，提供完整的笔记管理和艾宾浩斯复习功能。

## 🚀 技术栈

- **React 18** - UI 框架
- **TypeScript** - 类型安全
- **Vite** - 构建工具
- **TailwindCSS** - 样式框架
- **Zustand** - 状态管理
- **Axios** - HTTP 客户端
- **React Router v6** - 路由管理
- **Lucide React** - 图标库

## ✨ 功能特性

### 1. 用户认证系统
- ✅ 精美的登录/注册页面（渐变背景设计）
- ✅ JWT Token 自动管理（localStorage）
- ✅ 401 自动跳转登录
- ✅ 路由守卫（PrivateRoute）

### 2. 今日任务页面（首页）
- ✅ 显示今天需要复习的所有笔记任务
- ✅ 卡片式布局展示任务信息
- ✅ 分类标签、复习阶段、监督标识
- ✅ AI 总结预览（前 100 字符）
- ✅ 空状态提示
- ✅ 下拉刷新功能

### 3. 复习功能页面 ⭐ 核心功能
- ✅ 完整笔记内容展示
- ✅ AI 总结展开/收起
- ✅ **监督机制倒计时**：
  - 橙色背景倒计时区域
  - 大字体显示剩余秒数
  - 倒计时期间按钮禁用
  - 进度条动画
  - 倒计时结束后按钮可用
- ✅ 三种复习结果按钮：
  - 记住了（绿色）→ REMEMBERED
  - 有点模糊（黄色）→ FUZZY
  - 忘记了（红色）→ FORGOTTEN
- ✅ 提交后显示下次复习时间 Modal
- ✅ 自动返回首页并刷新

### 4. 笔记管理页面
- ✅ 笔记列表（卡片式布局）
- ✅ 左侧分类筛选（全部、未分类、各分类）
- ✅ 笔记详情页面：
  - 完整内容、分类、AI 总结
  - 复习记录展示
  - 编辑、删除功能
- ✅ 创建/编辑笔记表单：
  - 标题、内容输入
  - 分类选择下拉框
  - 监督机制开关
  - 监督时长输入（10-300秒）
  - 完整表单验证

### 5. 分类管理页面
- ✅ 分类列表卡片展示
- ✅ 创建分类 Modal
- ✅ 编辑分类 Modal
- ✅ 删除分类确认（带笔记检查提示）

## 📁 项目结构

```
web-app/
├── public/                # 静态资源
├── src/
│   ├── api/              # API 接口层
│   │   ├── client.ts     # Axios 配置和拦截器
│   │   ├── auth.ts       # 认证 API
│   │   ├── notes.ts      # 笔记 API
│   │   ├── categories.ts # 分类 API
│   │   ├── tasks.ts      # 任务 API
│   │   └── reviews.ts    # 复习 API
│   ├── types/            # TypeScript 类型定义
│   │   ├── index.ts      # 统一导出
│   │   ├── auth.ts       # 认证类型
│   │   ├── note.ts       # 笔记类型
│   │   ├── category.ts   # 分类类型
│   │   ├── task.ts       # 任务类型
│   │   └── api.ts        # API 响应类型
│   ├── store/            # Zustand 状态管理
│   │   ├── auth.ts       # 认证状态
│   │   ├── note.ts       # 笔记状态
│   │   ├── category.ts   # 分类状态
│   │   └── task.ts       # 任务状态
│   ├── components/       # React 组件
│   │   ├── ui/          # UI 基础组件
│   │   │   ├── Button.tsx
│   │   │   ├── Input.tsx
│   │   │   ├── Card.tsx
│   │   │   ├── Modal.tsx
│   │   │   ├── Loading.tsx
│   │   │   └── Countdown.tsx
│   │   └── layout/      # 布局组件
│   │       ├── Header.tsx
│   │       ├── Layout.tsx
│   │       └── PrivateRoute.tsx
│   ├── pages/            # 页面组件
│   │   ├── auth/        # 认证页面
│   │   ├── home/        # 首页
│   │   ├── notes/       # 笔记页面
│   │   ├── categories/  # 分类页面
│   │   └── review/      # 复习页面
│   ├── utils/            # 工具函数
│   │   ├── storage.ts   # LocalStorage 封装
│   │   ├── date.ts      # 日期格式化
│   │   └── validators.ts # 表单验证
│   ├── App.tsx           # 主应用组件
│   ├── main.tsx          # 应用入口
│   └── index.css         # 全局样式
├── index.html            # HTML 入口
├── vite.config.ts        # Vite 配置
├── tailwind.config.js    # TailwindCSS 配置
├── tsconfig.json         # TypeScript 配置
└── package.json          # 项目依赖
```

## 🔧 安装步骤

### 前置要求
- Node.js 16+ 
- npm 或 yarn

### 安装依赖

```bash
cd web-app
npm install
```

## 🚀 启动方式

### 开发模式

确保后端 API 服务已启动（默认 http://localhost:8080），然后运行：

```bash
npm run dev
```

应用将在 http://localhost:5173 启动。

Vite 已配置代理，所有 `/api` 请求会自动转发到后端服务。

### 预览构建结果

```bash
npm run preview
```

## 📦 构建部署

### 构建生产版本

```bash
npm run build
```

构建产物会生成在 `dist/` 目录。

### 部署到静态服务器

将 `dist/` 目录内容部署到任何静态文件服务器（Nginx、Apache、Vercel、Netlify 等）。

**重要配置**：需要配置服务器将所有路由重定向到 `index.html`（支持 React Router）。

#### Nginx 配置示例

```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /path/to/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # API 代理（生产环境）
    location /api {
        proxy_pass http://backend-server:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

## ⚙️ API 配置说明

### 开发环境

开发环境使用 Vite 代理，配置在 `vite.config.ts`：

```typescript
server: {
  proxy: {
    '/api': 'http://localhost:8080'
  }
}
```

### 生产环境

生产环境需要配置反向代理或修改 `src/api/client.ts` 中的 `baseURL`：

```typescript
const client = axios.create({
  baseURL: 'https://your-api-domain.com/api',
  // ...
});
```

## 🎨 UI/UX 设计

### 颜色方案
- 主色调：蓝色 (blue-600)
- 成功/记住了：绿色 (green-500)
- 警告/有点模糊：黄色 (yellow-500)
- 错误/忘记了：红色 (red-500)
- 监督倒计时：橙色 (orange-500)
- 背景：灰白色 (gray-50)

### 响应式设计
- 手机端 (< 640px)：单列布局
- 平板端 (640px - 1024px)：两列布局
- 桌面端 (> 1024px)：三列布局

### 动画效果
- 页面切换：淡入淡出 (fade-in)
- 按钮点击：轻微缩放 (scale)
- Modal 弹出：中心放大 (scale-in)
- 倒计时：进度条动画

## 🛠️ 开发指南

### 添加新页面

1. 在 `src/pages/` 下创建页面组件
2. 在 `src/App.tsx` 中添加路由
3. 使用 `<PrivateRoute>` 包裹需要认证的页面

### 添加新 API

1. 在 `src/types/` 中定义类型
2. 在 `src/api/` 中创建 API 函数
3. 在相应的 Zustand store 中调用

### 状态管理

使用 Zustand 进行状态管理：

```typescript
import { useAuthStore } from './store/auth';

const { user, isAuthenticated, setAuth } = useAuthStore();
```

### 表单验证

使用 `src/utils/validators.ts` 中的验证函数：

```typescript
import { validateNoteTitle } from './utils/validators';

const error = validateNoteTitle(title);
if (error) {
  // 显示错误
}
```

## 🔐 安全注意事项

- ✅ JWT Token 存储在 localStorage
- ✅ 401 错误自动清除 Token 并跳转登录
- ✅ Axios 拦截器自动添加 Authorization 头
- ✅ 所有 API 调用都有错误处理
- ⚠️ 生产环境请使用 HTTPS
- ⚠️ 定期更新依赖以修复安全漏洞

## 📝 代码质量

- ✅ TypeScript 严格模式
- ✅ 组件单一职责（< 200 行）
- ✅ 完整的类型定义
- ✅ 错误边界处理
- ✅ Loading 状态管理
- ✅ 用户友好的错误提示

## 🐛 常见问题

### Q: 启动后无法访问后端 API？
A: 确保后端服务已启动在 http://localhost:8080，或修改 `vite.config.ts` 中的代理配置。

### Q: 登录后刷新页面需要重新登录？
A: 检查浏览器是否禁用了 localStorage，或清除浏览器缓存。

### Q: 倒计时不工作？
A: 确保笔记创建时启用了监督机制并设置了监督时长。

### Q: 生产构建后路由 404？
A: 需要配置服务器将所有路由重定向到 index.html。

## 📄 许可证

本项目仅供学习和研究使用。

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

**开发者**: AI Plan Notes Team  
**文档更新**: 2024
